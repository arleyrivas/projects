import { DecimalDotPipe } from './decimal-dot.pipe';

describe('DecimalDotPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalDotPipe();
    expect(pipe).toBeTruthy();
  });
});
